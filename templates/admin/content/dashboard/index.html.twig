{% extends 'admin/base.html.twig' %}

{% block content %}

    <div class="page-header">
        <div><h1 class="page-title">{{ 'Dashboard'|trans }}</h1>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('admin') }}">{{ 'Admin'|trans }}</a></li>
                <li class="breadcrumb-item active" aria-current="page">{{ 'Dashboard'|trans }}</li>
            </ol>
        </div>

        {% include 'admin/page/pageheader.html.twig' %}

    </div>

    <div class="row">
        <div class="col-sm-12 col-md-12">
            <div class="card">
                 <div class="card-body">
                    Welcome to the Admin Panel!
                </div>
            </div>
        </div>
    </div>

{% endblock %}
